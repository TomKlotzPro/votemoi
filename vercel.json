{
  "version": 2,
  "buildCommand": "NODE_OPTIONS='--experimental-fetch' prisma generate && prisma migrate deploy && next build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ],
  "env": {
    "NODE_OPTIONS": "--experimental-fetch"
  }
}
