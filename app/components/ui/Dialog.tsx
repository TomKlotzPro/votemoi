import * as DialogPrimitive from '@headlessui/react';

export const Dialog = DialogPrimitive.Dialog;
export const DialogPanel = DialogPrimitive.Panel;
export const DialogTitle = DialogPrimitive.Title;
